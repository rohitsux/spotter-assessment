[pytest]
DJANGO_SETTINGS_MODULE = spotter_api.settings
python_files = test_*.py
testpaths = tests
addopts = -ra -q
markers =
    live_ors: requires real ORS API key; gated on ORS_LIVE=1 env var. Run with: ORS_LIVE=1 pytest -m live_ors
