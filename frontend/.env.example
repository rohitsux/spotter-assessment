# Frontend env. Local dev needs NO variables (Vite proxies /api → :8000).
# In production (Vercel), set VITE_API_BASE_URL to the Railway backend URL.
# Example:
# VITE_API_BASE_URL=https://spotter-backend-production.up.railway.app/api
